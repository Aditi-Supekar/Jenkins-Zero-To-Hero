pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Practice') {
            steps {
                echo 'Jenkins pipeline running successfully'
            }
        }
    }
}
